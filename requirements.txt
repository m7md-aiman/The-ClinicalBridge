# ClinicalBridge — Python dependencies (Python 3.11+)
# Install into the project venv:  .venv\Scripts\python -m pip install -r requirements.txt

# --- Core LLM / orchestration (LangChain) ---
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4        # used as the OpenRouter (OpenAI-compatible) chat client
langchain-community>=0.3,<0.4

# --- Retrieval-Augmented Generation: vector store + local embeddings ---
# Installed/used from Phase 7 onward. OpenRouter has no embeddings endpoint, so
# embeddings run locally via sentence-transformers (free, offline after first download).
langchain-chroma>=0.1,<0.3
chromadb>=0.5,<0.7
langchain-huggingface>=0.1,<0.3
sentence-transformers>=2.7

# --- Data / utilities ---
pydantic>=2.5,<3
python-dotenv>=1.0
tenacity>=8.2                     # retry logic for LLM calls

# --- Showcase website backend (web/) ---
fastapi>=0.115
uvicorn>=0.30
sse-starlette>=2.0                # SSE streaming for live pipeline runs

# --- Testing ---
pytest>=8.0
httpx>=0.27                       # FastAPI TestClient
